{
  "name": "bolao-copa-2026",
  "version": "1.0.0",
  "private": true,
  "description": "Bolão da Copa do Mundo 2026 — site público + admin online no Vercel",
  "scripts": {
    "build": "node build.js",
    "test": "node test/points.test.js && node test/api.local.js && node test/auth.local.js"
  },
  "dependencies": {
    "@upstash/redis": "^1.34.0"
  },
  "engines": {
    "node": "22.x"
  }
}
